# API-INTEGRATION-AND-DATA-VISUALIZATION
This script fetches a 5-day weather forecast for a specified city from the OpenWeatherMap API and visualizes the temperature trend over time using Matplotlib. It prompts the user to enter a city name, retrieves forecast data with proper error handling, parses timestamps and temperatures, and then displays a clear, labeled line chart of temperature.
